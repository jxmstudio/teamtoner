import type { Article } from "./types";

/**
 * Property Insights articles are written in the studio (/studio → Property
 * Insights); nothing ships as a fixture, so the section — and its menu entry —
 * stays hidden until the client publishes the first one.
 */
export const articles: Article[] = [];
